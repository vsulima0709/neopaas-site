# 03 — Replication Prompt: NeoPaaS × Sunday

```
INPUT:    v4 index.html copy (locked)
TARGET:   Sunday visual UX/UI
OUTPUT:   v5 index.html — same words, restyled
```

## Instructions to the build (this is what I executed in `index.html` v5)

1. **Type scale up.** Hero and section H2s bump ~20%. Section padding bumps to clamp(96px, 12vw, 180px). Sunday breathes more than v4.

2. **Hero becomes a two-column editorial spread on desktop.**
   - Left 6 cols: italic serif phrase + sans line + sub + CTAs
   - Right 6 cols: full-height photo slot (4:5)
   - Bottom: 4-up stat row with 1px ink rules above each label

3. **Section eyebrows become numbered editorial markers.** `01 —`, `02 —`, etc., in mono uppercase 12px.

4. **Cards across all sections get photo slots at top** (4:5 ratio), then small label, then sentence. No more ornamental letter glyphs (a. b. c.).

5. **Compliance section moves from solid clay surface → cream surface with forest-green eyebrow and H2.** Three cards become flat cream blocks with 1px borders.

6. **Today-vs-NeoPaaS table becomes two stacked editorial columns.** One column for "Today," one for "With NeoPaaS." Each item is a one-liner, not a table row. Sunday avoids tables.

7. **Partner strip restyled** — three editorial cards on cream with forest-green accents, no longer solid green panels.

8. **Final CTA: bigger, more breathing room, italic serif again as final emotional moment.**

## Output file

The actual updated wireframe is at `/Users/volodymyrsulima/Bab Innovations/Outputs/neopaas-site/index.html` (v5). The full implementation follows.
